> Generated by scribe on 2026-06-04 against commit 7a29198. If code contradicts this doc, trust the code and re-run scribe.

# CODE_MAP — карта файлов проекта

> Дерево/факты выверены по исходникам. Drift fixed (vs прошлой версии): нет `src/live_predictor.py` (ML живёт в `base/signal_wrappers.py`, модели в `ml-models/`); нет `ELO/run_series_experiment.py` (ELO теперь интегрирован в live через `ELO/live_team_strength.py`); hero-getters лежат в `base/dota2protracker.py`, не в `functions.py`; STAR-метрики и env-список переписаны под реальный код.

## Дерево (верхний уровень)

```
base/        # основной код аналитики и live runtime
ELO/         # ELO-модель силы команд (используется live + offline)
data/        # калибровки/пороги: star_thresholds_by_wr.json, star_confidence_calibration.json
ml-models/   # .pkl-артефакты phase-wrapper моделей (на сервере/runtime; локально может отсутствовать)
bets_data/   # логи ставок + public-матчи для backtest
pro_heroes_data/, hero_dota2protracker_data/  # кэши pro-данных
runtime/     # scratch: lock-файлы, очереди, журналы — git-ignored
docs/        # эта база знаний
```

---

## `base/cyberscore_try.py` — ⭐ live runtime (27 301 строк)

> Не читай целиком. `rg`/`grep` по имени функции + точечное чтение по диапазону.

Главный модуль: live-парсинг драфтов (CyberScore через Camoufox; DLTV api/html fallback), bookmaker prefetch (Camoufox subprocess + Selenium fallback), draft-метрики, networth/STAR/tier gates, ML phase-wrapper, dispatch в Telegram (+ VK mirror), runtime-менеджмент (lock, sharded/sqlite stats, delayed-очереди, recovery journal).

### Entrypoint (строки ~27123–27300)
- `__main__`: парс CLI → `_apply_live_entrypoint_pipeline_defaults()` → acquire instance lock → `while True: general(...)` + `_sleep_interruptible(...)` по возвращённому статусу.
- Цель цикла: `_POLL_CYCLE_TARGET_SECONDS` (default = `SCHEDULE_NEAR_MATCH_POLL_SECONDS` = 30с); спит остаток до цели.

### CLI-флаги
| Флаг | Значение |
|---|---|
| `--odds` / `--no-odds` | вкл/выкл odds-пайплайн (`args.odds`; None=из env `BOOKMAKER_PREFETCH_ENABLED`) |
| `--bookmaker-gate-mode {odds,presence}` | режим bookmaker-gate при включённых odds |
| `--dltv-source {api,html,cyberscore}` | источник live (default `cyberscore`) |
| `--pure-dltv` | отключить весь bookmaker prefetch/presence, только DLTV draft |

### Ключевые функции (имя — строка — назначение)
| Функция | ~строка | Назначение |
|---|---|---|
| `general(return_status=None, use_proxy=None, odds=None, bookmaker_gate_mode=None)` | 26884 | один цикл проверки матчей: загрузка состояния, fetch, draft-парс, метрики, gates, dispatch; возвращает статус (`None`/`series_finished`/`__sleep_*__`) |
| `_apply_live_entrypoint_pipeline_defaults()` | 1444 | проставляет дефолты пайплайна под выбранный режим/источник |
| `_get_cyberscore_html_via_camoufox(url=None)` | 18642 | one-shot Camoufox fetch HTML CyberScore |
| `_ensure_camoufox_browser()` | 7930 | ленивый старт/переиспользование Camoufox (bookmaker prefetch) |
| `_bookmaker_prefetch_loop()` | 8626 | фоновый воркер prefetch кэфов/presence |
| `_bookmaker_prefetch_submit(...)` | 8391 | поставить задачу prefetch |
| `_bookmaker_prefetch_lookup(match_key, wait_seconds=0.0)` | 7451 | забрать результат prefetch (с ожиданием) |
| `_bookmaker_prefetch_fetch_subprocess(...)` | 8462 | Camoufox в subprocess (основной путь при `BOOKMAKER_CAMOUFOX_ENABLED`) |
| `_bookmaker_prefetch_fetch_camoufox_direct(...)` | 8567 | прямой in-process Camoufox fetch |
| `_extract_nearest_cyberscore_scheduled_match_info(html)` | 10332 | ближайший матч расписания (sleep-таргет) |
| `_get_team_tier(team_id)` | 11503 | tier 1/2/3 по `id_to_names` (см. ARCHITECTURE) |
| `_match_has_tier1_team(radiant_team_id, dire_team_id)` | 11964 | True если ≥1 команда Tier-1 (OR; vs `_determine_star_signal_match_tier`=обе). Гейт kills-ставок (`KILLS_REQUIRE_TIER1_TEAM`) |
| `_stake_multiplier_for_signal(...)` | 4785 | множитель ставки (x0.5/1/2/3) — см. ARCHITECTURE «Stake multiplier» |
| `_build_stake_multiplier_context(...)` | 4908 | контекст для множителя |
| `_should_delay_star_signal(...)` | 3564 | решение отложить STAR-сигнал |
| `_networth_monitor_hold_check(...)` | 3612 | networth-hold перед отправкой |
| `_evaluate_kills_gate(...)` | 2441 | early-kills gate |
| `_try_acquire_runtime_instance_lock(mode_label=...)` | 1754 | single-instance lock на режим |
| `_load_star_confidence_calibration()` | 1574 | загрузка `data/star_confidence_calibration.json` |

### ELO live-интеграция (строки ~330–339)
`from ELO.live_team_strength import finalize_live_series_from_scores, get_matchup_summary, register_live_map_context`; `ELO_LIVE_SNAPSHOT_AVAILABLE` зависит от успешного импорта. ELO даёт matchup-summary для отображения, но **в stake-множителе ELO-gate удалён** (см. ARCHITECTURE).

### Импорты из `functions`
`send_message`, `drain_telegram_admin_commands`, `synergy_and_counterpick`, `calculate_lanes`, `format_output_dict`, `STAR_THRESHOLDS_BY_WR`, `STAR_DISABLED_METRICS`, `TelegramSendError`.
Из `keys`: `api_to_proxy`, `BOOKMAKER_PROXY_URL`, `BOOKMAKER_PROXY_POOL`, `DLTV_PROXY_POOL`.
Dota2ProTracker подгружается динамически (`importlib`) → `enrich_with_pro_tracker`.

### Режимы (через env, читаются `_env_flag`)
| Режим | env | Эффект |
|---|---|---|
| classic pipeline | `CLASSIC_SIGNAL_PIPELINE_ENABLED` (default `1`, `0` если minimal) | полные словари+метрики+gates |
| minimal odds-only | `SIGNAL_MINIMAL_ODDS_ONLY_MODE=1` | только команды+счёт+кэфы; словари/метрики/gates выключены |
| dota2protracker-only | `DOTA2PROTRACKER_ONLY_MODE=1` | минимальное сообщение только d2pt; включает `DOTA2PROTRACKER_BYPASS_GATES` по умолчанию |
| pure DLTV | `--pure-dltv` / `PURE_DLTV_MODE=1` | без bookmaker prefetch/presence |

### Переменные окружения (имя → default)

**Источник / Camoufox / прокси**
| env | default | назначение |
|---|---|---|
| `DLTV_SOURCE_MODE` | `cyberscore` | источник live (`api`/`html`/`cyberscore`) |
| `USE_PROXY` | — | вкл/выкл прокси (иначе True) |
| `DLTV_CAMOUFOX_ENABLED` | `1` если Camoufox доступен | Camoufox-first для DLTV html |
| `CYBERSCORE_CAMOUFOX_HUMANIZE` | `true` | гуманизация курсора |
| `CYBERSCORE_CAMOUFOX_OS` | `windows,macos` | OS fingerprint (CSV; `linux` исключён — browserforge No-headers) |
| `CYBERSCORE_CAMOUFOX_LOCALE` | `` (auto geoip) | принудительный locale |
| `CYBERSCORE_CAMOUFOX_WINDOW` | `` (auto random) | размер окна |
| `CYBERSCORE_CAMOUFOX_PROXY_URL` | `` | принудительный прокси Camoufox |
| `CYBERSCORE_CAMOUFOX_PROFILE_DIR` | `` | persistent profile dir |
| `CYBERSCORE_CAMOUFOX_REQUIRE_PROXY` | `1` | требовать прокси (direct запрещён) |
| `CYBERSCORE_CAMOUFOX_FETCH_ATTEMPTS` | `2` | попыток fetch |
| `LIVE_PROXY_PREFLIGHT_URL` | `CYBERSCORE_MATCHES_URL` | URL preflight-проверки прокси |
| `LIVE_PROXY_PREFLIGHT_CONNECTIVITY_URL` | `https://api.ipify.org` | connectivity-check |
| `LIVE_PROXY_PREFLIGHT_ATTEMPTS` | `3` | попыток preflight |
| `LIVE_PROXY_PREFLIGHT_TIMEOUT_SECONDS` | `12.0` | таймаут preflight |
| `PROXY_POOL_ROTATION_ROUNDS` | `3` | раундов ротации пула |
| `CYBERSCORE_EXTRA_TOURNAMENT_IDS` | внутр. default | доп. турниры |
| `CYBERSCORE_COMBINED_TIER_LISTING` | `1` | один листинг-фетч `tier=1,2,3,4` вместо tier1,2 + extra-id + name-tier (фильтр не-primary тиров по id/name client-side из match-row `tournament.{id,name,tier}`); `0` → legacy multi-fetch |
| `CYBERSCORE_COMBINED_PRIMARY_TIERS` | `1,2` | тиры, принимаемые безусловно в combined-режиме |
| `CYBERSCORE_COMBINED_TIERS` | `primary ∪ EXTRA_NAME_TIERS ∪ {3,4}` (обычно `1,2,3,4`) | набор тиров в combined листинг-URL; extra-id турнир вне набора подхватывается top-up фетчем (без потери live-матча) |

> Combined-режим сокращает per-cycle листинг-фетчи с 3 до 1 (`_get_cyberscore_heads_via_combined_listing` → `_classify_cyberscore_card_admission` → safety-net top-up для отсутствующих extra-id), снижает длительность цикла и ускоряет re-poll отложенных матчей.

**Bookmaker prefetch**
| env | default |
|---|---|
| `BOOKMAKER_PREFETCH_ENABLED` | False (если `--odds`/`--no-odds` не задан) |
| `BOOKMAKER_CAMOUFOX_ENABLED` | `1` |
| `BOOKMAKER_PREFETCH_GATE_MODE` | `odds` |
| `BOOKMAKER_PREFETCH_MODE` | `live` |
| `BOOKMAKER_PREFETCH_SITES` | `betboom,pari,winline` |
| `BOOKMAKER_PREFETCH_MESSAGE_WAIT_SECONDS` | `3.0` |
| `BOOKMAKER_PREFETCH_SUBPROCESS_TIMEOUT_SECONDS` | `160` |
| `BOOKMAKER_PREFETCH_DRIVER_ROTATE_TASKS` | `3` |
| `BOOKMAKER_PREFETCH_MAX_PENDING` | `200` |
| `BOOKMAKER_PREFETCH_RESULT_TTL_SECONDS` | `1800` |
| `BOOKMAKER_MATCH_TAB_CACHE_MAX_MATCHES` | `8` |
| `BOOKMAKER_PREFETCH_USE_SUBPROCESS` | авто при Camoufox |

**Dota2ProTracker**
| env | default |
|---|---|
| `DOTA2PROTRACKER_ENABLED` | `1` |
| `DOTA2PROTRACKER_ONLY_MODE` | `0` |
| `DOTA2PROTRACKER_BYPASS_GATES` | `1` если only-mode |
| `DOTA2PROTRACKER_SKIP_BOOKMAKER_GATE` | `1` если bypass |
| `DOTA2PROTRACKER_MESSAGE_BLOCK_ENABLED` | `1` если enabled |
| `DOTA2PROTRACKER_MIN_GAMES` | `10` |
| `DOTA2PROTRACKER_CP1VS1_GATE_ABS` | `3` |
| `DOTA2PROTRACKER_DUO_GATE_ABS` | `7` |
| `DOTA2PROTRACKER_PRELOAD` | `1` |

**Signal / STAR / ML decision**
| env | default |
|---|---|
| `CP1VS2_TOPUP_FALLBACK` | `0` (off) — вкл same-role top-up для `counterpick_1vs2`: дуо-матчап с `<COUNTERPICK_1VS2_MIN_MATCHES` точных игр добирается кросс-позиц (core pos1-3 / support pos4-5) агрегатом с весом λ, exact-игры как якорь (`functions._lookup_cp1vs2_topup_winrate`). Покрытые `>=15` не трогаются. Требует scoped-ключи: `cyberscore._draft_stats_lookup_keys` эмитит кросс-позиц дуо-ключи под тем же флагом. Валидировано (runtime/cp1vs2_topup_*): PUB ~+2пп точн.+покрытие, PRO нейтрально+~5-6пп покрытие. |
| `CP1VS2_TOPUP_LAMBDA` | `0.35` — вес кросс-позиц инкремента в top-up бленде |
| `SIGNAL_DECISION_MODE` | `ml` (`ml`/`star`) |
| `SIGNAL_WRAPPER_MODE` | `ml` |
| `SIGNAL_WRAPPER_ENABLED` | задаётся пайплайном вокруг вызова метрик |
| `STAR_THRESHOLD_WR_TIER1` | `60` |
| `STAR_THRESHOLD_WR_TIER2` | `60` |
| `STAR_LEVEL_MIN` | `60` |
| `STAR_LEVEL_MAX` | `90` |
| `ML_SIGNAL_MIN_WR` | `STAR_LEVEL_MIN` |
| `ML_CONFIDENCE_SOURCE` / `_EARLY` / `_LATE` | — |
| `STAR_BASELINE_EARLY_COVERAGE` | `41.09` |
| `STAR_BASELINE_LATE_COVERAGE` | `45.84` |
| `HERO_POSITION_STATS_MIN_PERCENTAGE` | `1` |
| `TEMPO_OVER_SCORE_THRESHOLD` | `0.9965` |
| `TEMPO_OVER_SCORE_LABEL` | `Ставка >=51` |
| `TEMPO_W_SOLO_KILLS` | `0.2414` | вес solo-kill индекса в score-формуле |
| `TEMPO_W_DUO_KILLS` | `0.2492` | вес duo-kill индекса |
| `TEMPO_W_CP_KILLS` | `0.2931` | вес cp1v1-kill индекса |
| `TEMPO_W_CP_DEATHS` | `0.3517` | вес cp1v1-death индекса (наибольший по logistic regression) |
| `TEMPO_INDEX_OFFSET` | `20` | offset для `max(0, idx - offset)` в score |
| `TEMPO_MIN_FOUND_FRACTION` | `1.0` | минимальная доля найденных ключей (1.0 = all-or-nothing; 0.8 ≈ ×2 покрытие) |
| `TEMPO_STOMP_FLOOR` | `1.2500` | нижний порог при ELO stomp (delta>150), не снижается hero-корректировками |
| `TEMPO_STAKE_HIGH_MARGIN` | `0.2535` | отступ от applied\_threshold для high-stake банды |
| `TEMPO_STATS_BLEND_MODE` | `False` | если True + `TEMPO_STATS_PUB_DIR` задан — блендировать pub+pro словари (Bayesian prior) |
| `TEMPO_STATS_PUB_DIR` | `` | путь к pub-словарям для blend-режима |
| `TEMPO_STATS_BLEND_PRIOR_STRENGTH` | `20` | сила pub-prior в blend (K в posterior) |
| `TEMPO_OVER_SENT_TTL_SECONDS` | `43200` | TTL дедуп-словаря `_tempo_over_sent_urls` (12 ч) |

**Networth gate (числовые пороги — частичный список)**
`NETWORTH_GATE_ALL_ONLY_EARLY_WINDOW_START_SECONDS` (240), `NETWORTH_GATE_ALL_ONLY_EARLY_MIN_DIFF` (800), `NETWORTH_GATE_SAME_SIGN_LANE_ADV_STALE_GRACE_SECONDS` (120), `NETWORTH_MONITOR_HOLD_SECONDS` (0). Большинство networth-порогов — модульные константы (`NETWORTH_GATE_*`, строки ~1233+), не env.

**Kills dispatch («СТАВКА НА Ранние килы»)**
| env | default | назначение |
|---|---|---|
| `LANE_ADV_STANDALONE_KILLS_ENABLED` | `1` | standalone lane_adv_dict≥6 kills-ставка (вне star-блока) |
| `KILLS_REQUIRE_TIER1_TEAM` | `1` | kills-ставки шлются ТОЛЬКО если ≥1 команда матча в Tier-1 (`id_to_names.tier_one_teams`). `0` → прежнее поведение (kills для всех). Гейт: `_match_has_tier1_team(r_id,d_id)` (OR-семантика, в отличие от `_determine_star_signal_match_tier` = обе Tier-1) применяется к `tier1_early_kills_mode` и к `_try_dispatch_lane_adv_standalone_kills`. |

**Schedule / quiet hours** — см. `docs/SCHEDULING.md`.

**Stats backend / sharding**
| env | default |
|---|---|
| `STATS_DIR` | `ANALYSE_PUB_DIR` |
| `STATS_EARLY_PATH`/`_LATE_PATH`/`_LANE_PATH`/`_POST_LANE_PATH` | `{stats_dir}/*_dict_raw.json` |
| `STATS_LOOKUP_BACKEND` | `auto` |
| `STATS_SHARDED_LOOKUP_MODE` | `auto` |
| `STATS_SHARDED_LOOKUP_MAX_RAM_GB` | `8.0` |
| `STATS_WARMUP_STEP_DELAY_SECONDS` | `45.0` |
| `STATS_SQLITE_BUILD_BATCH_SIZE` | `50000` |
| `STATS_SQLITE_QUERY_CHUNK_SIZE` | `800` |

**Runtime paths**
| env | default |
|---|---|
| `MAP_ID_CHECK_PATH` | `~/.local/state/ingame/map_id_check.txt` (единый для всех режимов) |
| `RUNTIME_INSTANCE_LOCK_PATH` | `runtime/cyberscore_try.instance.lock` |
| `DELAYED_QUEUE_PATH` | `runtime/delayed_signal_queue.json` (суффиксируется режимом) |
| `SENT_SIGNAL_JOURNAL_PATH` | `runtime/sent_signal_recovery.jsonl` |
| `SENT_SIGNAL_JOURNAL_FALLBACK_PATH` | `runtime/sent_signal_recovery_fallback.jsonl` |
| `UNCERTAIN_SIGNAL_DELIVERY_PATH` | `runtime/uncertain_signal_delivery.jsonl` |
| `TEMPO_STATS_DIR` | `TEMPO_EXPERIMENT_DIR` |

> При чистке мёртвых прокси в `base/keys.py` трогай только runtime proxy constants/pools; **НЕ** трогай `api_to_proxy`/`api_to_keys` и API-ключи.

---

## `base/functions.py` (~9000 строк) — общие хелперы метрик + dispatch

| Функция | ~строка | Сигнатура / назначение |
|---|---|---|
| `send_message` | 1502 | `send_message(message, *, require_delivery=False, admin_only=False, mirror_to_vk=True)` — Telegram-рассылка подписчикам (+ VK mirror), бросает `TelegramSendError` на жёстком фейле |
| `drain_telegram_admin_commands` | 929 | `drain_telegram_admin_commands(*, refresh=True) -> list[dict]` — забрать admin-команды бота |
| `synergy_and_counterpick` | 3760 | `synergy_and_counterpick(radiant_heroes_and_pos, dire_heroes_and_pos, early_dict, mid_dict, match=None, custom_weights=None, early_trio_threshold=SYNERGY_TRIO_MIN_MATCHES, mid_trio_threshold=SYNERGY_TRIO_MIN_MATCHES, synergy_duo_use_max=False, early_position_weights=None, late_position_weights=None, post_lane_dict=None, post_lane_trio_threshold=POST_LANE_SYNERGY_TRIO_MIN_MATCHES, post_lane_position_weights=None)` — основной расчёт synergy/counterpick по словарям |
| `calculate_lanes` | 8105 | `calculate_lanes(radiant_heroes_and_pos, dire_heroes_and_pos, heroes_data, merge_side_lanes=False, return_sources=False, core_support_side_lanes=False)` — top/bot/mid lane-метрики |
| `format_output_dict` | 1962 | `format_output_dict(output_dict, flag=False, none_trashold=None, target_wr=None, late_signal_gate_enabled=None)` — форматирование блока + флаг наличия STAR-hit |

> **Option C — post_lane `solo` (2026-06):** `post_lane_output['solo']` теперь ЭМИТИТСЯ (раньше был выключен `name != 'post_lane_output'`). Сам post_lane-словарь: cp/synergy/trio на широком окне, а **solo-записи (`{hero}pos{n}`) собираются ТОЛЬКО на последнем version-патче** (`analise_database._add_combinations_to_dict(write_solo=...)`, `LATEST_PATCH_START_TS` из `keys.DOTA_VERSION_PATCH_EVENTS`, сейчас 7.41d=1780531200). Solo уже в `STAR_SIGNAL_METRICS` и в sign-consistency `all_output` → автоматически участвует как сигнал в All-блоке; добавлен в display-списки. Порог `SOLO_MIN_MATCHES=50` (покрытие на 7.41d ≈95%).

**Константы STAR (источник истины для решений):**
- `STAR_SIGNAL_METRICS = {'counterpick_1vs1', 'counterpick_1vs2', 'dota2protracker_cp1vs1', 'solo'}` (frozenset, строка 1880).
- `STAR_DISABLED_METRICS = frozenset()` (пусто).
- `STAR_THRESHOLD_SECTIONS = ('early_output', 'mid_output', 'all_output')`.
- `STAR_THRESHOLDS_BY_WR = _load_star_thresholds()` ← `data/star_thresholds_by_wr.json` (env `STAR_THRESHOLDS_PATH`).
- `STAR_THRESHOLD_WR` env default `60`.

> Drift fixed: `synergy_duo`/`synergy_trio` НЕ входят в `STAR_SIGNAL_METRICS` (могут показываться в блоках, но не решают STAR).

---

## `base/signal_wrappers.py` (~2000 строк) — ML phase-wrapper (ДЕЙСТВУЮЩАЯ ML)

> Drift fixed: это и есть «ML-модели» проекта, не `src/live_predictor.py`.

Применяет sklearn-модели (`.pkl`) поверх ранних/поздних draft-метрик, сдвигая STAR-индексы.

**Публичный API:**
- `apply_early_signal_wrapper(phase_bucket, radiant_heroes_and_pos, dire_heroes_and_pos, phase_context=None) -> None` (1983) → `_apply_phase_wrapper(phase="early", ..., enabled_env_name="SIGNAL_WRAPPER_EARLY_ENABLED")`.
- `apply_late_signal_wrapper(phase_bucket, radiant_heroes_and_pos, dire_heroes_and_pos, phase_context=None) -> None` (1999) → phase="late", `SIGNAL_WRAPPER_LATE_ENABLED`.
- `clear_wrapper_caches() -> None` (532).

**Модели/пути (константы):**
- `DEFAULT_ML_MODEL_SET_DIR = PROJECT_ROOT/"ml-models"/"phase_models_early66_latebase"`.
- early: `phase_signal_wrapper_early.pkl`; late: `phase_signal_wrapper_late.pkl` (+ legacy пути в `ml-models/`).

**env:** `SIGNAL_WRAPPER_ENABLED` (False), `SIGNAL_WRAPPER_MODE` (`ml`), `SIGNAL_WRAPPER_DEBUG`, `SIGNAL_WRAPPER_CROSS_PHASE_STAR_FEATURES` (False), `SIGNAL_WRAPPER_EARLY_ENABLED`/`_LATE_ENABLED`, `SIGNAL_WRAPPER_CONFIG_PATH`, `SIGNAL_WRAPPER_ML_EARLY_PATH`/`_LATE_PATH`, `SIGNAL_WRAPPER_REQUIRED_SKLEARN_VERSION`, `HERO_FEATURES_PATH`, `STAR_THRESHOLD_WR` (60), `STAR_THRESHOLDS_PATH`.

Внутренние gate-функции (early/late hard-carry, support-gap, control-stability, role-balance, edge-requirements и т.д.) — `_apply_*_gate(...)` строки 1127–1882.

---

## `base/dota2protracker.py` (~1500 строк) — pro matchups/synergy + hero-registry

Парсер dota2protracker.com (Camoufox/Selenium против Cloudflare). **Здесь живут hero-getters проекта.**

| Функция | ~строка | Сигнатура |
|---|---|---|
| `get_hero_id` | 140 | `get_hero_id(hero_name: str) -> int` (OpenDota ID; 0 если нет) |
| `get_hero_name` | 155 | `get_hero_name(hero_id: int) -> str` |
| `get_hero_slug` | 189 | `get_hero_slug(hero_name: str) -> str` |
| `get_hero_registry` | 125 | `() -> dict` — из `base/hero_features_processed.json` |
| `parse_hero_matchups` | 626 | `parse_hero_matchups(hero_name, use_cache=True, ...)` — fetch+cache matchups/synergies |
| `enrich_with_pro_tracker` | 1361 | `enrich_with_pro_tracker(radiant_heroes_and_pos, dire_heroes_and_pos, synergy_dict, min_games=10) -> Dict` — добавляет `pro_cp1vs1_*`, `pro_duo_synergy_*`, `*_valid`, `*_games`, `*_reason` |
| `calculate_lane_advantage` | 1231 | `calculate_lane_advantage(radiant_positions, dire_positions, hero_data, min_games=10, core_support_side_lanes=False) -> Dict` |
| `_get_matchup_1v1` | 1121 | направление с большим числом игр |
| `_get_duo_synergy_pair` | 1205 | duo для пары позиций |

**Lane-маппинг (константы `LANE_CP1VS1_PAIRS`/`LANE_DUO_PAIRS`, строки ~74–110):**

| Лейн | cp1vs1 (radiant×dire) | min matchups | duo synergy |
|------|-----------------------|--------------|-------------|
| **mid** | pos2 vs pos2 | 1 | нет |
| **top** | pos3 vs pos1, pos3 vs pos5, pos4 vs pos1, pos4 vs pos5 | 2 | radiant pos3+pos4 vs dire pos1+pos5 |
| **bot** | pos1 vs pos3, pos1 vs pos4, pos5 vs pos3, pos5 vs pos4 | 2 | radiant pos1+pos5 vs dire pos3+pos4 |

Прочее: `DUO_VALID_THRESHOLD=0.8`, `PRO_POSITION_COVERAGE_THRESHOLD=2/3`. Результат `cp1vs1_valid` требует валидности всех 3 лейнов; `duo_valid` — top+bot. Кэш: `hero_dota2protracker_data/`.

---

## `base/hero_features_processed.json` — профили героев (schema v6, патч 7.41c)

Единый источник признаков героя: dict `{hero_id_str: row}`, 127 героев × 119 полей. Курируется вручную (multi-agent прогон 2026-06-13, веб-верификация по 7.41c); в репо генератора НЕТ — правки руками/агентами не перезатираются. `patch_tag=curated_2026_06_xpick_axes_v6`, `schema_version=6`. Бэкап v5: `*.bak_20260613_020144`.

Потребители: `signal_wrappers.py` (`_load_hero_features`, env `HERO_FEATURES_PATH`), `functions.py` (counterplay, env `HERO_FEATURES_FILE`/`COUNTERPLAY_HERO_FEATURES_PATH`), `dota2protracker.py` (registry), `runtime/xpick_dataset_*.py`. Все старые 77 полей v5 сохранены по именам/типам — действующие скореры/гейты не сломаны, но ЗНАЧЕНИЯ ~679 ячеек исправлены (чистка мусора вида `stun_max_duration=1500`, реклассификация контроля).

**Новое в v6 (поверх v5):**
- **Lockdown vs interrupt:** `has_lockdown/lockdown_count/lockdown_max_duration/lockdown_min_duration` (lockdown = удерживает врага, позволяя бить: стан ≥0.5с/hex/root/sleep/taunt/channel-привязка), `lockdown_reliability` ∈ {reliable,aoe,skillshot,channel,conditional,none}, `interrupt_only_count` (микро-станы ≤0.3с: AM Mana Void, Zeus Bolt и т.п. — НЕ считаются `has_stun`), `stun_min_duration`.
- **Taunt:** `has_taunt/taunt_count` (Axe Call, LC Duel, WW Curse).
- **Урон:** `damage_pattern` ∈ {burst,sustained,dot,mixed,utility}, `is_burst/burst_window_s`, `has_dot/dot_score`, `pure_damage_flag`.
- **19 осей-рейтингов 0-100** (калиброваны относительно всех 127; 50=средний, 93+=иконик): `laning/tempo/farm/teamfight/control/initiation/resilience/save/mobility/physical_damage/magic_damage/push/defense/fight_cooldown/macro/anti_bkb/burst/late/simplicity_score` + `hard_initiation/soft_initiation` (флаги), `initiation_reliability`.
- **Роли:** `positions` (list), `primary_position`, `lane_role` (list), `profile_notes` (тонкости героя, текст), `verified_patch`.

Назначение осей — строительные блоки для команд-агрегатов «цельность драфта / дыры» (нет инициации, нет сейва, нет масс-файта) поверх контрпиков/синержи. Конвенции спорных кейсов: Dazzle `has_hex=1` (7.41: Poison Touch хексит во время Nothl Projection), Invoker/BH без `has_stun` (микро-станы → `interrupt_only_count`), Kez без lockdown (shard-гейтнутый контроль не считаем).

---

## `base/id_to_names.py` — справочник команд (tier1/tier2)

- `tier_one_teams: dict {name: team_id | set[ids]}` (строка 1).
- `tier_two_teams: dict {name: team_id | set[ids]}` (строка 2).
- Значение может быть `int` или `set` (несколько Dota team_id под одним именем).
- `clean_team_names(...)` нормализует имена; внизу файла — auto-onboarded секции.
- Tier 3 = всё остальное (нет в обоих dict). См. `_get_team_tier` в cyberscore (строка 11503).

---

## `base/check_old_maps.py` — offline backtest draft-метрик

Прогоняет исторические/public карты через те же метрики, что в live-сигнале: словари `early/late/lane/post_lane_dict_raw`, d2pt `cp1vs1`/`duo`/`lane_advantage`, lane/early/late/post-lane outcomes.

**Ключевые функции:** `check_old_maps(...)` (692), `collect_matches(...)` (639), `_protracker_metrics_for_match(...)` (568), `_load_stats_dicts(...)` (381), `_resolve_maps_paths(maps_path, patch=None)` (99).

**CLI-аргументы:**
| arg | default |
|---|---|
| `--maps-path` | `DEFAULT_MAPS_PATH` (файл/csv-список/glob/директория со split-частями) |
| `--patch` | None (напр. `7.41` → start ts `1774310400`) |
| `--output` | `DEFAULT_OUTPUT_PATH` |
| `--stats-dir` | `DEFAULT_STATS_DIR` |
| `--start-date-time` | None |
| `--max-matches` | None |
| `--dicts` / `--no-dicts` | True |
| `--dota2protracker` / `--no-dota2protracker` | False |
| `--dota2protracker-min-games` | 10 |
| `--dota2protracker-refresh` | False |
| `--no-stale-dota2protracker-cache` | False |
| `--dota2protracker-sleep` | 0.0 |
| `--post-lane-max-cached-shards` | 48 (для больших public — поднимать, напр. 127; live не меняет) |
| `--merge-side-lanes` | False |
| `--core-support-side-lanes` / `--no-...` | True |
| `--disable-lanes` | False |

Известный sqlite-баг (kv vs stats table) — см. memory `backtest-sqlite-kv-table`. lane_adv_dict A/B — memory `lane-adv-dict-backtest`.

Пример 50k public (7.41):
```bash
/Users/alex/Documents/ingame/venv_catboost/bin/python3 base/check_old_maps.py \
  --maps-path bets_data/analise_pub_matches/json_parts_split_from_object \
  --patch 7.41 --max-matches 50000 --dicts --dota2protracker \
  --post-lane-max-cached-shards 127 \
  --output runtime/pub_7.41_50k_metrics.json
```

---

## `base/metrics_winrate.py` — bucket winrate из JSON `check_old_maps`

CLI: `--input` (precomputed JSON), `--bucket-mode`/`--old-mode`, `--min-matches` (6).
```bash
/Users/alex/Documents/ingame/venv_catboost/bin/python3 base/metrics_winrate.py \
  --input runtime/pub_7.41_50k_metrics.json --bucket-mode --min-matches 6 \
  > runtime/pub_7.41_50k_winrate.txt
```

---

## `base/bookmaker_selenium_odds.py` (~110KB) — bookmaker presence/odds/deeplink

Camoufox + Selenium-парсер кэфов/наличия матча (betboom/pari/winline). Умеет `presence`, `odds`, `deeplink`. При `BOOKMAKER_CAMOUFOX_ENABLED=1` subprocess-путь не строит Selenium для odds/deeplink. Класс результата `SiteResult` (878). Основные хелперы: `_run_presence_sites_in_camoufox` (744), `_extract_map_odds_deeplink` (1040), `_extract_market_map_num`/`_resolve_map_num_for_site` (нормализация номера карты).

---

## `base/keys.py` — структура (БЕЗ значений)

Telegram: `Token`, `Chat_id`, `Chat_ids`. VK: `VK_GROUP_ID`, `VK_PEER_ID`, `VK_PEER_IDS`, `VK_API_VERSION`, `VK_GROUP_TOKEN`. Steam: `STEAM_API_KEY`. Patch-таймстемпы: `start_date_time*`, `DOTA_PATCH_EVENTS`, `DOTA_PATCH_START_TIMES`, `DOTA_VERSION_PATCH_EVENTS`, `DOTA_PATCH_SPECS`. Прокси: `BOOKMAKER_PROXY_RAW/URL/FALLBACK`, `DLTV_BANNED_PROXIES`, `SHARED_PROXY_POOL`, `RU_PROXY_POOL`, `BOOKMAKER_PROXY_POOL`, `DOTA2PROTRACKER_PROXY_POOL`, `DLTV_PROXY_POOL`, `BOOKMAKER_PROXIES`. API-маппинги: `api_to_proxy` (+ `api_to_keys`). НЕ коммитить; пример — `keys_local.example.py`.

---

## `ELO/` — ELO силы команд (live + offline)

| Модуль | назначение |
|---|---|
| `live_team_strength.py` | live-снапшот силы: `register_live_map_context`, `finalize_live_series_from_scores`, `get_matchup_summary`, `DEFAULT_RUNTIME_PROGRESS_PATH` (импортируется в cyberscore) |
| `models.py` | ELO/рейтинговые модели |
| `domain.py` | `LeagueTier`, `MatchRecord` |
| `roster.py` | `RosterResolution`, roster-lock логика |
| `tiering.py` | классификация tier лиг/команд |
| `series_data.py` | группировка серий (`_series_group_key`) |
| `data_loader.py`, `team_identity.py`, `config.py` | загрузка/идентичность/конфиг |
| `live_probability_segment_policy.json` | политика сегментов вероятности |

> Drift fixed: `ELO/run_series_experiment.py` отсутствует. Запускать offline-эксперименты через существующие модули/тесты `ELO/tests/`.

---

## Тесты (`base/tests/`)

```bash
pytest base/tests/ -v
```
Файлы: `test_pipeline_integrity.py`, `test_networth_dispatch_gates.py`, `test_tier_threshold_switch.py`, `test_kills_gate_logic.py`, `test_same_sign_lane_adv_dispatch.py`, `test_all_only_watcher_dispatch.py`, `test_star_hits_summary.py`, `test_dota2protracker_integrity.py`, `test_post_lane_synergy_pipeline.py`, `test_compact_draft_metrics_logging.py`, `test_analise_database_post_lane.py`, `test_explore_database_integrity.py`, `test_betboom_rejection_fixtures.py`, `test_avito_monitor.py` (+ `fixtures/`).

## `base/tempo_analise_database_experiment.py` — билдер Tempo-Over словарей

Строит pub/pro словари `tempo_solo_dict_raw.json`, `tempo_synergy_duo_dict_raw.json`, `tempo_counterpick_1vs1_dict_raw.json` для предиктора «Ставка >=51». Используется `cyberscore_try.py` через `_load_tempo_stats_dicts()`.

### Публичные функции (сигнатуры)
| Функция | Назначение |
|---|---|
| `build_tempo_draft_metrics(radiant, dire, solo_dict, duo_dict, cp_dict, *, min_found_fraction=1.0)` | Считает 4 draft-индекса (solo_kills_pm, duo_kills_pm, cp_kills_pm, cp_deaths_pm). Возвращает dict с `complete`, `usable`, `found_fraction` по каждому семейству |
| `process_tempo_pub_match(match, solo_dict, duo_dict, cp_dict)` | Накапливает статистику из pub-матча (исключает про-матчи) |
| `process_tempo_pro_match(match, solo_dict, duo_dict, cp_dict, *, min_start_ts, tier_one_ids, strict_positions)` | Накапливает статистику из про Tier-1 матча |
| `load_tempo_dicts_blended(pro_dir, pub_dir, *, prior_strength)` | Bayesian blend: posterior = `(pro_sum + K×pub_mean)/(pro_games + K)` |
| `_record_mean_shrunk(record, metric, *, family_mean, min_games, prior_strength)` | Shrinkage-оценка (Bayesian posterior) — NEW; `_record_mean` не трогает |

### Env-константы (из `tempo_analise_database_experiment.py`)
| env | default | назначение |
|---|---|---|
| `TEMPO_MIN_GAMES` | `5` | min games для non-shrunk оценки |
| `TEMPO_PRIOR_STRENGTH` | `20` | сила prior в shrinkage (`K`) |

### Директории словарей
| Путь | Содержимое |
|---|---|
| `bets_data/tempo_pub_experiment/` | pub-словари (НЕ трогать) |
| `bets_data/tempo_pro_experiment/` | про Tier-1 словари + `meta.json` (строит `build_tempo_pro_dicts.py`) |

---

## `pro_heroes_data/build_tempo_pro_dicts.py` — построитель про-словарей

Фикс train/serve mismatch (issue #2): строит про Tier-1 словари в `bets_data/tempo_pro_experiment/`.

```bash
venv_catboost/bin/python3 pro_heroes_data/build_tempo_pro_dicts.py [--tier-mode team|premium_league|all] [--out-dir PATH]
```

- Атомарная запись (`.tmp` + `os.replace`); pub-словари **не трогает** (guard: refuses if out_dir == pub_dir).
- Пишет `meta.json` с provenance (git sha, n_matches, tier_mode, ts).
- После построения: `TEMPO_STATS_DIR=bets_data/tempo_pro_experiment` или blend-режим.

---

## `pro_heroes_data/tempo_revamp_backtest.py` — бэктест харнесс Tempo-Over

Параметризованный бэктест: coverage × WR × ELO-бакеты × min_found_fraction × источники словарей.

```bash
venv_catboost/bin/python3 pro_heroes_data/tempo_revamp_backtest.py \
  --dicts bets_data/tempo_pub_experiment --tier-mode team --folds 5 [--min-frac 0.8] [--json-out FILE]
```

- `--tier-mode {team,league,both,all}` — `team` повторяет serving (по `id_to_names.tier_one_teams`).
- Walk-forward по времени (`--folds N`); отчёт ALL vs FRESH-180d.
- Wilson CI; atomic JSON output → `runtime/tempo_revamp_backtest_<ts>.json`.

---

## Вспомогательные / исследовательские (не live)
`analise_database.py`, `explore_database.py`, `maps_research.py`, `networth_comeback_research.py`, `bookmaker_ocr.py`, `avito_monitor.py`, `signal_wrappers.py` (см. выше), `test.py`.
