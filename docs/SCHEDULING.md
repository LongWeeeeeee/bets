> Generated by scribe on 2026-06-03 against commit 10c583e. If code contradicts this doc, trust the code and re-run scribe.

# Schedule / Sleep Policy

Main-цикл (`base/cyberscore_try.py` `__main__`, ~27193) выбирает sleep по статусу `general()`. Цель цикла при активных матчах — `_POLL_CYCLE_TARGET_SECONDS` (env `CYBERSCORE_POLL_CYCLE_TARGET_SECONDS`, default = `SCHEDULE_NEAR_MATCH_POLL_SECONDS` = **30с**): после обработки спит остаток до цели.

## Env-параметры расписания (имя → default)

| env | default |
|---|---|
| `CYBERSCORE_SCHEDULE_POLL_SECONDS` | `30*60` (1800с) |
| `SCHEDULE_MAX_SLEEP_SECONDS` | `30*60` (cap сна) |
| `SCHEDULE_LONG_IDLE_THRESHOLD_SECONDS` | `30*60` |
| `SCHEDULE_WAKE_LEAD_SECONDS` | `30*60` (lead до старта матча) |
| `SCHEDULE_NEAR_MATCH_POLL_SECONDS` | `30` |
| `SCHEDULE_POST_START_POLL_SECONDS` | `3*60` (poll после старта, ожидание live) |
| `CYBERSCORE_POLL_CYCLE_TARGET_SECONDS` | `SCHEDULE_NEAR_MATCH_POLL_SECONDS` (30) |
| `CYBERSCORE_QUIET_HOURS_START_HOUR_MSK` | `0` |
| `CYBERSCORE_QUIET_HOURS_END_HOUR_MSK` | `6` |
| `CYBERSCORE_QUIET_HOURS_PROBE_ENABLED` | `1` |
| `CYBERSCORE_DELAYED_STALE_REFRESH_SECONDS` | `90` |

## Статусы → sleep

| Статус `general()` | Поведение |
|---|---|
| `None` / прочее | sleep = `max(0.5, target - elapsed)` (~30с цикл) |
| `series_finished` | fetch расписания → sleep до ближайшего матча или cap `SCHEDULE_MAX_SLEEP_SECONDS` |
| `__sleep_until_schedule__` | sleep ровно до ближайшего матча (`NEXT_SCHEDULE_SLEEP_SECONDS`) |
| `__sleep_wait_for_live_after_schedule__` | `SCHEDULE_POST_START_POLL_SECONDS` (3 мин) — матч стартовал, ждём live |
| `__sleep_cyberscore_quiet_hours__` | sleep до конца ночного окна (00:00–06:00 MSK) |

## Quiet hours (00:00–06:00 MSK)
В режиме `DLTV_SOURCE_MODE=cyberscore` в начале `general()` проверяется `_cyberscore_quiet_hours_sleep_seconds_with_probe()`. Если в окне — bookmaker prefetch воркер останавливается, runtime спит до конца окна (probe может разбудить раньше при появлении матча, если `CYBERSCORE_QUIET_HOURS_PROBE_ENABLED=1`).

## Заметки
- Все sleep — через `_sleep_interruptible(...)` (прерывается admin-командами/событиями).
- Цикл ~30с при активных матчах — норма; основное время уходит на Camoufox page loads (карточки + individual match pages) и bookmaker prefetch.

## Сбор публичных матчей (serv1, cron, с 25.09.2026)
Сбор пабликов STRATZ запускает root-cron на serv1, без агентов и Codex:
`23 * * * * /bin/bash /root/main/scripts/run/pub_recrawl_cron.sh >> /root/main/runtime/artifacts/pubs-rebuild/cron.log 2>&1`.
Обёртка срабатывает раз в час и пишет в `cron.log` одну строку: `skip busy` (замок `runtime/pub_recrawl.lock` занят) или `skip not_due` (обход завершён, 5 дней от его старта ещё не прошли). Во всех остальных случаях она пишет `launch` и вызывает `get_pubs_full_recrawl.sh`. Этот скрипт продолжает упавший или зависший обход с прежним cutoff и курсором, а после срока начинает следующий; `base/pub_recrawl.py` сам проверяет замок и пятидневный срок. После падения обход продолжается в течение часа.
Выключить: `crontab -e` на serv1, убрать строку. Слить осиротевшие temp-файлы без обхода: `flock -n runtime/pub_recrawl.lock venv/bin/python3 base/maps_research.py --merge-temp-files [--dry-run]`.

Список игроков для обхода — `bets_data/analise_pub_matches/pub_player_steam_ids.json` (объединение, только растёт).
В конце каждого обхода сборщик перечитывает файл и добавляет к нему id из собранных матчей (без анонимов и смурфов).
- Интервал 5 дней отсчитывается от **начала** прошлого обхода (`started_at`). Если обход растянулся дольше, следующий
  стартует сразу. Так было 26.09: обход 19–26.09 закончился в 03:27, следующий запустился в 04:23.
- 26.09.2026, по решению владельца («добавь»), в список добавлены аккаунты с лиговых карт за 180 дней:
  - источник — `runtime/artifacts/misc/pro_corpus_rich.npz`, 28 196 карт, 24 406 аккаунтов;
  - новых 11 602, всего 583 026;
  - набор лежит в `runtime/artifacts/pubs-rebuild/pro_seed_accounts_20260926.json` (sha 5f922cb2);
  - резервная копия — `pub_player_steam_ids.json.bak_before_pro_seed_20260926`.

  Шедший в тот момент обход загрузил список до добавления, поэтому новые аккаунты обходит следующий.
  Разовая добавка: новые про-игроки в список сами не попадают (E-339 §5), добавку повторять вручную.
- Слитые temp-файлы: `temp_files` после merge не чистится. 26.09 с согласия владельца удалены 5134 уже слитых
  файла (6,6 ГБ); удалялись только файлы не новее `merge_patch_summary.json`.
