# Сохранение живых результатов при пересборке ELO

## Инцидент 11.09.2026

На serv1 утренняя цепочка заменила ELO-снимок в 09:06 МСК, переписала
`runtime/live_elo_model_state.json` в 09:09 и собрала пустую дельту в 09:10.
При этом срез нового снимка — **03:37:50 МСК** (`1789087070`).
Прежняя перебазировка копировала только `model_state` нового снимка, а загрузчик
progress сбрасывал `pending_series` и `applied_maps` при смене базы.

В журнале есть успешные применения шести результатов после этого среза.
В кэшах Stratz и `prematch_live_delta.json` сохранились их исходы и десять
аккаунтов на каждую карту:

| Match ID | Окончание, МСК | Победитель |
|---|---|---|
| 8992639832 | 04:18:39 | Tеаm kolyakaban |
| 8992671196 | 05:27:52 | Tеаm kolyakaban |
| 8992727109 | 06:54:49 | Team Nemesis |
| 8992752853 | 07:24:08 | Team EternalSadness |
| 8992777443 | 08:06:29 | InterActive Philippines |
| 8992786293 | 08:24:50 | Team EternalSadness |

После пересборки `live_elo_delta.json` содержал `changes={}`, а `applied_maps`
был пуст. Следовательно, эти результаты не вошли ни в новый срез, ни в живой
слой. Локальные доказательства: `runtime/artifacts/elo/incident-20260911/`
(`evidence.json`, `verdict_context.json`). Они не публикуются и не коммитятся.

Получение результата тоже имеет задержку: для первых двух карт
InterActive Philippines — Team Nemesis Stratz разрешил исход через 19 и 18
минут соответственно. До пересборки обновление доходило до выдачи:
после первой карты Nemesis 2156 → 2159, InterActive 1906 → 1901.
Исправление перебазировки само по себе не ускоряет публикацию результатов.

Повторная проверка в 09:45 МСК показала новое применение карты `8992825325`
и непустую дельту с обновлениями десяти игроков. Процесс остался прежним
(`530488`, запуск в 09:11). Таким образом, обработка новых исходов работает,
но потерянные при пересборке шесть результатов не восстановлены. При ремонте
состояния нужно сохранить и этот более поздний результат.

## Контракт перебазировки

`ELO/live_team_strength.py` сохраняет у нового применённого результата полный
`match_record`, исход и время его применения моделью. При переходе на новый
срез состояние строится из нового снимка и результатов, которые в нём ещё
отсутствуют. Ожидающие карты сохраняются, если их ещё нет в снимке.

Время применения не доказывает отсутствие карты в срезе: запоздалый ответ
Stratz может касаться карты, уже попавшей в корпус. Поэтому новый снимок
содержит ограниченный список ID завершённых карт и границу его покрытия в
`meta.recent_completed_match_ids` и
`meta.recent_completed_match_ids_coverage_since`. Проверка использует точное
вхождение ID и временные границы; неоднозначность останавливает перебазировку
до записи файлов. Старые записи без достаточного контекста нельзя молча
отбросить или восстановить по одному имени команды.

CLI `ELO/rebase_runtime_model_state.py` принимает `--snapshot`, `--state` и
`--progress`. Все три пути относятся к одной операции. Повторный запуск на
той же базе сохраняет уже применённую дельту. Нормальный live-путь использует
ту же проверку при смене базы; прежний пустой progress не заменяет
неперенесённые результаты.

Запись завершённой карты отмечается в progress как незавершённая до сохранения
дельты или полного состояния. После сбоя следующий вызов восстанавливает
состояние из снимка и журнала результатов; обычная регистрация без такой
отметки не загружает большое полное состояние. Неоднозначная очередь,
в которой снимок покрывает лишь часть ожидающих карт, блокирует перенос:
старый счёт серии нельзя безопасно сопоставить с остатком очереди.

Для карт, уже учтённых снимком, progress хранит временные отметки дедупликации.
Их срок — семь дней от известного окончания карты либо первого подтверждения
снимком; следующая пересборка не продлевает срок. До истечения срока требуется
актуальное подтверждение ID новым снимком. Отсутствие подтверждения может
консервативно отклонить перенос, в том числе на границе окна покрытия.
Результат, который пришлось бы применить раньше или одновременно со срезом,
также отклоняется: время события не подменяется ради успешной пересборки.

`scripts/run/rebuild_prematch_snapshot.sh` доставляет новый снимок в `.tmp`.
После остановки сервиса он сначала выполняет безопасную перебазировку, затем
заменяет снимок и обновляет дельту/массивы. При отказе перебазировки снимок не
заменяется. CLI возвращает `1` при безопасном отказе с сохранённым прежним
состоянием; сервис запускается на прежнем снимке, цепочка возвращает ошибку.
Если откат не удался (`2`) или процесс аварийно прерван, сервис остаётся
остановленным до проверки целостности runtime-состояния.

## Проверка и восстановление

Регрессионные тесты находятся в `ELO/tests/test_runtime_state_memory.py` и
`ELO/tests/test_snapshot_delivery.py`. Проверяются перенос результатов,
повторный запуск, ID карт, уже учтённых снимком, сохранение ожидания и отказ без
перезаписи при недостаточном контексте. Доставка проверяется исполнением
реального shell-блока с изолированными заглушками команд.

Уже потерянные результаты автоматически не появляются после установки кода.
Для отдельного восстановления служит `ELO/recover_live_results.py`: входы
`--snapshot`, `--progress`, `--delta`, `--events`; выходы `--output-state`,
`--output-progress`, `--output-report` должны отличаться от входных путей.
CLI создаёт отдельное полное состояние и progress, не устанавливая их в runtime.
`--verify-current-delta` требует сначала воспроизвести все текущие применения
и сверить их с действующей дельтой. Затем текущие и восстановленные события
проигрываются вместе в хронологическом порядке. Новая дельта строится штатным
`ELO/convert_state_to_delta.py` из отдельного полного состояния.

Для шести карт инцидента сохранились время окончания источника, исход, составы
и команды, но исходное время live-применения потеряно. Восстановление использует
проверенное окончание по политике `finished_results_v1`, с provenance
`recovered_source_finish` и `original_applied_at_unknown=true`. Оно не обещает
побитового совпадения с утраченным состоянием, использовавшим время получения
ответа. Для двух более поздних применений обнаружено расхождение в одну секунду
между старым `applied_at` и фактическим временем модели: старый код вызывал часы
отдельно. Время модели восстановлено из frozen delta: все десять global,
десять local и две roster-отметки каждой карты должны совпасть. Исходное время
progress сохраняется в ledger как `original_progress_applied_at`, выбранное —
как `result_timestamp`, provenance — `current_delta_last_seen`. После этого
контроль требует полного совпадения replay с дельтой без допуска по времени.
Ожидающие карты сохраняются; неполный контекст, дубли или
незавершённая запись блокируют восстановление.

Установка требует остановленного сервиса, замороженных резервных копий всех
трёх runtime-файлов и проверки хешей снимка. Сначала устанавливается progress
с `pending_overlay_commit` для всех результатов, затем состояние и дельта,
последним — чистый progress. При прерывании эта отметка позволяет штатному
загрузчику восстановить целостное состояние из снимка и журнала. После проверки
используется `scripts/run/restart_cyberscore.sh`. Деплой и изменение runtime
требуют разрешения оператора.

## Результат восстановления 11.09.2026

После разрешения оператора код `c9f9d30` и `340322f` доставлен через origin.
Сервис остановлен, три runtime-файла, журнал и map-id guard сохранены в
`runtime/artifacts/elo/recovery-20260911-r1/before/` на serv1; SHA-256 снимка и
файлов записаны в manifest. Первый контроль отклонил секундное расхождение;
после восстановления фактических времён второй контроль совпал полностью.

Восстановлены шесть пропущенных результатов и сохранены два более поздних
(`8992825325`, `8992847821`). Ожидающая карта `8992864996` перенесена без
изменения очереди. Полное состояние и дельта совпали по общему контракту
Telegram/ML на всех восьми составах: максимальная ошибка `0.0`.
Дельта содержит 167 изменённых записей в десяти полях. Пересчёт занял 84 с,
конвертация — 71 с; максимальный RSS — около 5 ГиБ.

Установка завершена с receipt `INSTALLED`; после запуска штатным скриптом
сервис активен с 10:36:14 МСК, PID `554767`. Отдельный вызов рабочего
`get_matchup_summary` после установки подтвердил чтение новой дельты и
совпадение рейтингов с проверенными значениями. Артефакты `report.json`,
`validation.json`, `install_receipt.json`, `postdeploy.json` и логи находятся
в том же каталоге восстановления; локальные копии отчётов — в каталоге
инцидента. Это подтверждает восстановление указанных восьми событий, но не
означает независимый аудит полноты всей истории ELO.
