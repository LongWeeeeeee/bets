# Локальная оркестрация Codex и Claude Code

Проверено 2026-09-06. Настройка относится к этому Mac; отдельные клоны и серверы
не получают её автоматически. Правила совместной работы: `AGENTS.md`, раздел 4,
и `/Users/alex/Documents/AGENTS.md`, раздел 7.

## Общая доска

Оба приложения используют одну реализацию в `~/.claude/hooks/`:
`board-shared.mjs`, `board-inject.mjs`, `board-gate.mjs`. Корень определяется
через Git common directory: вложенные каталоги и linked worktree используют
основную `.beads/beads.db`. Независимый вложенный репозиторий не наследует
чужую доску. Содержимое чатов автоматически общим не становится.

Стартовый хук печатает путь БД и actor `codex:<session_id>` либо
`claude:<session_id>`. Перед изменениями нужно прочитать задачу, получить её
через атомарный `update <id> --claim` и указать свои файлы. Чужой claim нельзя
перезаписывать. Один файл — один автор; claim не заменяет файловую блокировку.

После изменений Stop проверяет обновление задачи текущим actor, её assignee
и новый ID события в SQLite. Обновление другой сессии не засчитывается.
Проверка замечает прямые Edit/Write/apply_patch и явные sed/perl с `-i`;
произвольные shell/Python-записи требуют соблюдения протокола самим агентом.
При ошибках координация явно помечается degraded; число повторных требований
ограничено двумя. Хуки не являются границей безопасности.

Beads работает напрямую с SQLite и выгружает JSONL без демона. В обоих проектах
установлено `no-daemon: true`. Два старых демона остановлены, повторное чтение
БД не запустило их. `.beads/` остаётся локальной: никаких `bd sync` или push.

## Контекст и лишняя работа

- Codex: `model_auto_compact_token_limit = 160000`.
- Claude: `autoCompactWindow = 200000`; подсказки о контексте работают для всех
  моделей, на 150000 и 180000 токенов, с повторным предупреждением после compaction.
- Автокоммиты всего дерева и принудительный reflect-gate отключены в регистрации
  и по умолчанию в самих скриптах. Коммиты выполняются явно по своим файлам.
- Короткие независимые задания субагентам; нет обязательной квоты делегирования
  или цепочки ролей. Выбранные модели и reasoning не менялись.

## Активация и проверки

**Установка не означает активацию.** Read-only API Codex `hooks/list` обнаружил
все пять регистраций в `~/.codex/hooks.json`, но вернул `trustStatus: untrusted`.
Пользователю нужно открыть `/hooks` в Codex, просмотреть и подтвердить команды.
Если в desktop команда недоступна, открыть Codex CLI в этом проекте и вызвать
`/hooks` там. Затем возобновить задачу либо начать новую сессию. База доверия
вручную не менялась. Для Claude открыть новую сессию после изменения настроек.

Проверка без вызовов модели:

```sh
/Users/alex/Documents/ingame/venv_catboost/bin/python3 ~/.codex/hooks/orchestration_v3/doctor.py
bash ~/.claude/hooks/tests/board-gate-cases.sh
bash ~/.claude/hooks/tests/board-gate-ownership-roots.sh
/Users/alex/Documents/ingame/venv_catboost/bin/python3 ~/.claude/hooks/tests/test_context_cost.py
/Users/alex/Documents/ingame/venv_catboost/bin/python3 ~/.claude/hooks/tests/test_disabled_callbacks.py
bd daemon list
```

Результат: doctor — 0 ошибок; 76 сценариев доски, 21 сценарий владения/worktree,
6 тестов контекста и 2 теста отключённых callbacks прошли. Контрольные запуски
установленного inject прочитали реальные доски aitrading и ingame от обоих
harness: верные DB, actor и команда claim. Это проверка обработчиков;
автоматическое исполнение Codex после выдачи доверия ещё нужно подтвердить.

Конфигурация и код сохранены в существующей локальной Git-истории
`~/.claude/journal-history`: до изменения `ac72119`, после `5e15fc7`.
У истории нет remote. В прод, торговые процессы и существующий монитор ресёрча
изменения не вносились. Снижение фактического расхода токенов новым прогоном
ещё не измерено.
