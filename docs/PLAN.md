# План работ — от 06.08.2026

> Составлен по итогам ревизии сессий 05–06.08 (E-01…E-26 в `docs/EXPERIMENTS.md`).
> Правило чтения: фазы идут по приоритету; внутри фазы пункты независимы, если
> не указана зависимость. Каждый пункт кончается DoD — без него пункт не закрыт.

---

## Фаза 0 — сегодня: мелочи, защищающие всё остальное (~1.5 ч)

**0.1. Включить запись истории котировок Winline.** Сейчас история живёт в
памяти поллера (`self._attempts`) и умирает с процессом — на диске её нет.
Append-jsonl: ts, match/map, кэфы обеих сторон, статус рынка. Данные копятся
только вперёд, без этого фаза 3.2 не начнётся никогда.
Осторожно: `runtime/winline_current_map_odds_poller.py` на serv1 разошёлся с
локальной копией — править на serv1, деплой при следующем рестарте поллера.
*DoD: файл растёт на живом матче.* ~40 мин.

**0.2. Прочитать ночные зонды** (`probe_shared_quota`, `probe_position_null`,
логи в `runtime/artifacts/pubs-rebuild/`). Раздельные квоты = +20% скорости
сбора (шестая пара); ответ про 791k `position:null` = природа ¾ всего отсева.
*DoD: выводы в журнале.* 15 мин.

**0.3. Мониторинг эффекта E-18 в проде** (через сутки от деплоя). Доля `all=ok`
в WR60-оценках против базы **40.1%**; hit-строки `Protracker_solo/duo (WR60)`;
частота `sign_consistency_override`. Грепалка готова (в E-18).
*DoD: вердикт в E-18 — оставить пороги / поднять duo.* 15 мин.

**0.4. Воспроизводимость дня.** Все харнессы и артефакты (бейзлайны, ELO,
сила игроков, дампы holdout) лежат в gitignored `runtime/` на одном ноутбуке,
журнал на них ссылается. Тарболл в `runtime/_archive/runs/` + rsync ключевых
артефактов на serv1. *DoD: архив существует, путь записан в журнал.* 20 мин.
Отдельно — **решение alex**: трекать ли `runtime/experiments/` в git (меняет
конвенцию раскладки) или остаёмся на архивах.

**0.5. Мина `check_old_maps.py`.** Чужой незакоммиченный рефактор (+1125/−853)
ломает сбор локальных тестов и **уедет в первый же `sync.sh`**.
**Решение alex:** доделать в той сессии / откатить / закоммитить как есть.

**0.6. Метка `positions_repaired` в починке позиций при сборе** (3 строки).
Вступит при следующем рестарте сбора; через месяц даст выборку для проверки,
не делает ли починка перестановку неверно. *DoD: закоммичено.* 15 мин.

---

## Фаза 1 — измерительные дыры (1–2 дня)

**1.1. Починить харнесс №0** (раздел 2 журнала): выбор окна = максимальный
`|ed|` среди прошедших `min_ed` (как прод, `not_strongest_window`), не первое
по времени. Пересчитать: games-гейт, 1v1-vs-with, зеркальный matched-volume,
судьбу `lt_star`. *DoD: обновлённые числа в журнале, дефект закрыт.* ~3 ч.

**1.2. kills-вариант E-25 — главная незакрытая дыра.** «Фильтрация — тупик»
доказана для All (min_games=100); kills_window живёт на **min_games=10** и
демонстрирует чувствительность к объёму (зеркало: удвоение выборки → −18%
ставок на фикс-пороге). Собрать kills_window в трёх вариантах
(`EXPLORE_METRICS=kills_window`, base / nostrict / pkg, ~20 мин каждый),
прогнать через **исправленную** прод-политику на про-дампе: n ставок / WR /
ROI. Зависимость: после 1.1. *DoD: вердикт в журнал — либо «тупик»
закрывается окончательно, либо решаем, что включать.* ~полдня.
Опция: если kills покажет чувствительность — тот же тест для Late.

---

## Фаза 2 — что разблокировала насыщенность (2–3 дня)

**2.1. Патч-срезы словарей.** Словарь мешает 5 месяцев меты; данных теперь с
запасом (E-25). Собрать post_lane только на 7.41d+e с порогами 30/30/10
(с исключением holdout), сравнить с полным 7.41+ на holdout: AUC / WR /
покрытие. *DoD: число; при плюсе — прод-отбор перед внедрением (правило №1).*
~полдня. При успехе — то же для late / kills отдельным шагом.

---

## Фаза 3 — новое измерение (главная ставка, ~неделя)

**3.1. Цена как вероятность.** Единственный источник, ортогональный драфту и
NW; сейчас кэфы — только гейт доставки. Шаги: аудит, что уже фиксируется в
момент сигнала (sent/shadow-логи) → датасет сигнал+кэф+исход → разрез ROI по
расхождению нашей оценки с implied probability (маржу снять по паре).
*DoD: ответ «эдж концентрируется по расхождению с ценой или нет».* 2–3 дня.
Можно параллелить с фазой 2 — данные не пересекаются.

**3.2. Движение линии как контр-сигнал.** Питается пунктом 0.1; замер после
1–2 недель накопления: сдвиг линии против нас к дедлайну → пропуск/вето.
*DoD отложенный.*

---

## Фон (ожидание, не активная работа)

- **Сбор паблика:** фаза 1 ещё ~2 суток, фаза 2 ~5 суток (потолок — квоты
  Stratz, не код). После: корпус вниз (эталон — serv1), production rebuild,
  **нарезать новый holdout** — текущие 50k окажутся внутри прод-словарей.
- **30 падений тестов на serv1** — отдельная сессия на полдня.
- **Диск serv1** (43 ГБ свободно) — следить во время фазы 2 сбора.

## Не делаем (закрыто, не расползаться)

Фильтрация матчей сверх п. 1.2 (E-20…E-26); λ top-up (E-02); вес надёжности и
зеркальный пулинг (E-07/E-09); WR65+ для протрекерных метрик (E-18); своя
разметка смурфов (E-23); расширенная починка позиций (E-24).
