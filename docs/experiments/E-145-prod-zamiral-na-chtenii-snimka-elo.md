---
id: E-145
title: "Прод замирал не на матчах, а на чтении 349-МБ снимка ELO: вторая копия в памяти"
date: "2026-08-13"
area: prod
status: full
corpus: "лог прода 1.16 млн строк за сутки; журнал вердиктов 400 карт; лог монитора Winline 41.9 тыс. записей; py-spy dump прод-процесса"
verdict: "Пропажа второй карты BoomBoys из tail_log — не дефект журнала и не фильтр моста. Прод стоял: main-thread висел в `read_text` на `ELO/output/live_team_elo_snapshot.json` (349 МБ) внутри per-match пути `check_head -> record_shadow_observation -> load_roster_history`. Историю килов держит тот же файл, который `live_team_strength` уже загрузил в `_SNAPSHOT_CACHE`, а shadow-модуль грузил ВТОРУЮ независимую копию: RSS 4.0 ГБ + 6.85 ГБ свопа при 8191/8191 занятого свопа машины, `STAT=Dsl`, лог не рос 33 минуты, разрывы 5-19 мин весь день. Матч OG-BoomBoys карта 2 (match_id 8943055466) лежал в мосту 33 минуты — монитор видел его 20 раз, прод не упомянул НИ РАЗУ. Фикс: переиспользуем загруженный снимок + потолок 64 МБ на файл истории. После: цикл 50-100 c вместо 5-19 мин, обе живые карты в журнале"
harness: "py-spy dump; ~/.local/state/ingame/map_verdicts.json; runtime/winline_parser_monitor.log"
---

# E-145. Прод замирал на чтении снимка ELO

- **Дата:** 2026-08-13 (alex: «я всё ещё не могу через tail_log получить вторую
  карту boomboys»)

## 1. Три версии подряд оказались неверными

Проверялись и отпали: группировка карточек в `tail_log` (починена в
`cb52ccf`, но записи всё равно не было), фильтр лиг в мосту, «прод не успевает
дойти до третьего матча». Последняя выглядела подтверждённой — в хвосте лога
было 2 блока `SourceTV match #` при трёх живых матчах, — но это оказался
артефакт короткого окна: на полном логе `голов == блоков` во всех циклах.

## 2. Матч был в мосту и не был у прода

`sourcetv_probe` завёл матч в 07:56:30 и удалил в 08:53:56. Монитор Winline,
читающий тот же `runtime/sourcetv_matches.json`, видел его **20 раз** с 08:19
по 08:52 — с корректной картой:

```
{"match_id": 8943055466, "team1": "OG", "team2": "BoomBoys", "map_num": 2}
```

В логе прода за те же 57 минут `grep -c 8943055466` = **0**.

## 3. Причина: main-thread стоял в чтении файла

```
Thread 3082097 (idle): "MainThread"
    read_text (pathlib.py:1030)
    _load_roster_history_cached (base/team_kills25_shadow.py:343)
    load_roster_history -> build_roster_kills_context -> record_shadow_observation
    _record_team_kills25_shadow_candidate (base/cyberscore_try.py:6031)
    check_head -> general
```

`STAT=Dsl` (непрерываемое ожидание диска), CPU 3:43 за 41:47 аптайма,
RSS 4.0 ГБ + `VmSwap` 6.85 ГБ, своп машины 8191/8191 МБ.

Файл — `ELO/output/live_team_elo_snapshot.json`, **349 МБ**. Читателей два:
`ELO/live_team_strength.py` (кладёт в `_SNAPSHOT_CACHE`) и
`base/team_kills25_shadow.py` (свой `lru_cache`). История килов лежит ВНУТРИ
снимка ELO, поэтому shadow-модуль разворачивал вторую независимую копию той же
структуры. Отсюда ~11 ГБ на 16-ГБ машине и своп-трэшинг.

Независимое подтверждение из журнала вердиктов: дыра **08:38:20 -> 09:25:58**,
ровно окно жизни второй карты BoomBoys.

## 4. Что сделано

`load_roster_history` берёт готовый объект из `live_team_strength._SNAPSHOT_CACHE`,
если путь совпадает, и файл повторно не парсит. Плюс потолок
`TEAM_KILLS27_ROSTER_HISTORY_MAX_MB` (64 МБ): файл крупнее в живой путь не
берётся, контекст ростера деградирует до `history_unavailable` (штатная ветка
`build_roster_kills_context`), а не вешает цикл. Тесты: 23 passed на serv1.

| замер | до | после |
|---|---|---|
| RSS прода | 4.0 ГБ | 2.8-3.6 ГБ |
| VmSwap прода | 6.85 ГБ | 4.9-5.4 ГБ |
| своп машины | 8191/8191 | 7040/8191 |
| длина цикла | 5-19 мин, заморозки до 33 мин | 50-100 c |
| живых матчей в цикле | 1 из 2-3 | все |

## 5. Разнос артефактов замером НЕ оправдался

На диске история килов — 90% файла (`team_kills_history_by_team_id` начинается
на байте 34 748 278 из 365 578 581), и напрашивался вывод «вынести её —
освободить гигабайты». **В памяти доля обратная.** Замер на локальной копии
того же снимка (`json.load`, затем замена истории на `{}` + `gc.collect()`):

| | RSS |
|---|---|
| до чтения | 11 МБ |
| после `json.load` | 1819 МБ |
| без истории килов | 1560 МБ |
| **цена истории** | **259 МБ (14%)** |

71 503 команды в истории занимают 259 МБ, а 1.56 ГБ — это `teams_by_org_key` и
`model_state`, то есть сам ELO. Разнос артефактов дал бы ~0.26 ГБ из ~8.5 ГБ
рабочего набора прода и латентность не чинит. Гипотезу закрываем.

Ключ трогать нельзя ещё и технически: `ensure_snapshot` (строка 1122) считает
`snapshot_missing_kills_history` основанием для ПОЛНОЙ пересборки снимка —
удаление ключа запустило бы rebuild-цикл. Допустимо только `{}`.

## 6. Настоящая причина задержки ставок — cgroup-лимит и партиционирование

После фикса §4 прод всё равно оценивал матч раз в 90–180 c. Два замера:

1. **`MemoryHigh=4G`/`MemoryMax=6G`** (`oom.conf`, стоял с 12.07) при рабочем
   наборе ~9 ГБ: ядро непрерывно выдавливало страницы, **307 556 major-faults
   за 180 c** (~1700/с). Поднято до 10G/13G (`OOMScoreAdjust=-900` сохранён) —
   стало **1 015 за 180 c**, своп прода 5.6 ГБ → 0.18 ГБ.
2. Но частота оценок не выросла. `py-spy` по 14 сэмплам: **10 из 11 активных
   сэмплов главного потока — в `structure_lane_dict`** (`base/functions.py:349-359`),
   вызываемой из `calculate_lanes` и `calculate_lane_kills_advantage` на каждый
   матч. Функция каждый раз проходила по всему плоскому lane-словарю
   (~7 млн ключей при `LANE_CELL_VALUE=nw_mean`, 5.5 ГБ RSS) и строила пять
   новых словарей.

Оба потребителя и так конвертируют вход через гейт `'2v2_lanes' not in ...`,
поэтому словарь партиционируется **один раз при загрузке** (`_load_stats_dicts`),
а плоская таблица освобождается. Значения объектов те же самые — память
нейтральна.
