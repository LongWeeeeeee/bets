---
id: E-331
title: "Темп серии: несут ли прошлые карты той же серии информацию о килах сверх модели B"
date: "2026-09-24"
area: ml
status: partial
corpus: "тест модели B (E-329, B_d86400_noOD, карты с 2026-03-29, 20 326–24 209 карт по целям); серии из pro_corpus_rich.npz (sids, 68% карт с серией); продолжения серии — 2 926–3 589 карт (14,4–14,9%)"
verdict: "Предрегистрированный замер (8 признаков серии) критерий не прошёл: total −0,0018 LL [−0,0048; +0,0012], rad +0,0008. Post-hoc по одному признаку на цель: для тотала среднее число килов прошлых карт серии даёт −0,0029 LL [−0,0055; −0,0005] на картах 2+ (AUC +0,0035), по всем картам −0,0004 [−0,0008; −0,00005]; для rad не доказано (−0,0007, ДИ включает 0); для окон эффекта нет. B занижает P(тотал ≥55) на поздних картах серии: разрыв +1,5 / +2,4 / +5,6 п.п. по терцилям килов прошлых карт. Вывод: темп серии несёт сигнал для тотала; окна от него не выигрывают."
harness: "runtime/experiments/kills/series_tempo/{residual_probe.py,posthoc_probe.py}"
---

# E-331. Темп серии поверх модели B

## 1. Вопрос (владелец, 24.09.2026)

«Учитываем ли мы на второй карте первую, а на третьей две предыдущие? Так можно уловить темп
серии: если команды играют аккуратно с малым количеством фрагов, скорее всего это сохранится».

Факт до замера: не учитываем.
- Прод-панель A: 928 колонок (sym, F6, F7, F8, prod35, kwdict, publogit, rating, hybrid), блока серии нет.
- Модель B: история kills-v3 видна только при `end < start − 86400` (ночной снимок), поэтому
  сегодняшние карты серии ей невидимы.

## 2. Предрегистрация (`.omc/decisions.md`, до чисел)

- **Популяция:** тестовые карты B, у которых ≥1 карта той же серии закончилась до старта.
- **База:** перекрёстная логистика `y ~ logit(b)`. Это только перекалибровка: она убирает
  выигрыш от самой переподгонки.
- **Кандидат:** база + признаки серии: число прошлых карт, средний и последний тотал, темп
  кил/мин, килы своей и чужой команды, разница килов в окне.
- **Проверка:** 5 фолдов по дням, парный бутстреп по дням (1 000 повторов).
- **Критерий опровержения:** верх ДИ ≥ 0 и для total, и для rad.
- **Моё предсказание:** total −0,003…−0,008, окна ≈ 0.

## 3. Результат предрегистрированного замера — не подтверждён

| цель | продолжений | ΔLL кандидат − база | ДИ95 |
|---|---|---|---|
| total_55_50 | 3 555 | −0,00183 | [−0,00477; +0,00120] |
| rad_30_25 | 3 519 | +0,00083 | [−0,00176; +0,00332] |
| w_5_15 | 3 589 | +0,00171 | [−0,00067; +0,00402] |
| w_15_25 | 3 422 | +0,00243 | [+0,00122; +0,00381] |

Окна хуже базы. Это переобучение: 8 признаков на 3,5 тыс. карт стоят около 8/3 400 ≈ 0,002 LL.

Описательно B занижает тотал на продолжениях серии, и сильнее — после кровавых карт:

| терциль среднего тотала прошлых карт | карт | факт P(≥55) | среднее B |
|---|---|---|---|
| 19–54 | 1 204 | 0,624 | 0,609 |
| 54–70 | 1 227 | 0,748 | 0,724 |
| 70–150 | 1 124 | 0,864 | 0,808 |

## 4. Post-hoc (придуман ПОСЛЕ чисел §3)

Один признак на цель, выбран по смыслу без перебора: тотал — средний тотал прошлых карт;
rad — килы команды, которая сейчас за Radiant; окна — её перевес по килам в том же окне.
Модель по всем картам: `logit(b) + продолжение + номер карты + продолжение × признак`.

| цель | ΔLL, все карты | ΔLL, карты 2+ | ΔAUC, 2+ |
|---|---|---|---|
| total_55_50 | −0,00041 [−0,00080; −0,00005] | −0,00293 [−0,00548; −0,00053] | +0,0035 |
| rad_30_25 | −0,00009 [−0,00030; +0,00014] | −0,00067 [−0,00208; +0,00085] | +0,0015 |
| w_5_15 | −0,00001 | −0,00012 [−0,00183; +0,00143] | +0,0022 |
| w_15_25 | +0,00012 | +0,00081 [+0,00036; +0,00125] | −0,0018 |

Уровень (продолжение + номер карты без темпа) значим не оказался. Разрыв «факт − B» по тоталу растёт
с номером карты: bo3, карта 2 — +2,4 п.п. (n = 2 312), карта 3 — +5,3 п.п. (n = 646).

**Механизм (гипотеза, не проверено):** тотал задаёт пара команд сегодня — стиль обеих, патч,
ставки. Прошлые карты серии — самая близкая выборка именно этой пары. У B есть только долгие
командные ставки килов с отставанием в сутки. Окна не выигрывают, потому что победителя окна решает
драфт, а он на каждой карте новый. Проверка механизма — признак «тотал прошлых карт» против
«тотал той же пары в прошлых сериях»: если выигрывает только сегодняшняя серия, решает «сегодня».

Согласуется с E-329: цена суточной задержки сидит на тех же 14,5% карт и сильнее всего на тотале
(+0,0068).

## 5. Что это меняет

- В Telegram-панели показываются только окна и dur43; `total_55_50` и `rad_30_25` идут только в
  журнал. Видимой прибавки панели признак сейчас не даст.
- Прибавку он даст тоталу — и модели kills ≥30, если будет в неё встроен.
- Нужен живой блок «килы прошлых карт серии» (счёт прошлых карт виден боту в живом фиде) и
  переобучение тотала с этим признаком. Отдельная задача на доске.

## 6. Харнесс и команды

```
venv_catboost/bin/python3 runtime/experiments/kills/series_tempo/residual_probe.py   # предрегистрированный
venv_catboost/bin/python3 -W ignore runtime/experiments/kills/series_tempo/posthoc_probe.py
```

Выход: `runtime/artifacts/kills/series_tempo/{residual_probe,posthoc_probe}.json`.
Вход: `runtime/artifacts/kills/panel_plus_v3/full_cov_s1_d86400_noOD/<key>.predictions.npz`,
`runtime/artifacts/misc/pro_corpus_rich.npz`.

## 7. Где искать ошибку

- **Серии** берутся по `sids > 0` из rich-корпуса (68% карт). Карты без серии считаются первыми.
  Если `sids` путают серии (одна пара дважды за день), «прошлые карты» смешаются.
- **«Прошлая карта»** — та, что закончилась до старта: `ts + durations < ts_текущей`.
  Единицы `durations` приняты за секунды. Если это не так, фильтр пропустит или впустит лишние карты.
- **Ориентация по командам** требует, чтобы у всех прошлых карт была та же пара id. Иначе
  командные признаки обнуляются; так 1,5% продолжений у тотала.
- **Килы** считаются как сумма `pstats[:, :, 0]` (игроки 0–4 — Radiant), окна — по `rk/dk` на
  минутной сетке 0–40.
- **Post-hoc и 6 целей:** одна значимая цель на уровне 5% может быть случайностью. Подтверждение —
  градиент по терцилям и совпадение с E-329, а не один ДИ.
- **Предупреждения `matmul`** в выводе — ложные сообщения numpy 2.0 с Accelerate на macOS.
  Сами LL конечны и согласованы.

## 8. Продолжение 24.09 вечером: механизм, калибровка, форма поправки

**Механизм — сегодняшняя серия, а не стиль пары** (`mechanism_probe.py`, предрегистрация в
`.omc/decisions.md` до чисел). Поверх `logit(b) + продолжение + номер карты`:

| сравнение | ΔLL | ДИ95 |
|---|---|---|
| темп сегодняшней серии, карты 2+ | −0,00293 | [−0,00548; −0,00053] |
| история пары в прошлых сериях (конец < старт − 86400, ≤365 дн., ≤10 карт), все карты | +0,00002 | [−0,00006; +0,00011] |
| сегодня поверх пары, карты 2+ | −0,00290 | [−0,00544; −0,00047] |
| пара поверх сегодня, карты 2+ | −0,00014 | ДИ включает 0 |

Корреляция признаков 0,29; история пары есть у 46% продолжений (медиана 3 карты). Ночной признак пары
сигнал не заменяет: нужны живые килы прошлых карт серии.

**Калибровка B по номеру карты** (разрыв «факт − среднее B» для P(тотал ≥55)): все карты −0,03 п.п.;
карта 1 −0,57; продолжения **+3,11** (se 0,73); карта 2 +2,67; карта 3 +5,20; первая/вторая половина
теста на продолжениях +2,73 / +3,42 — это не дрейф периода.

**Форма поправки** (`fit_correction.py`, OOF по дням на 3 555 продолжениях):
`logit p' = logit b + a + β·(средний тотал прошлых карт − 63,43)`, a = 0,240, β = 0,00996 на кил;
только уровень: a = 0,202.

| сравнение | ΔLL | ДИ95 |
|---|---|---|
| поправка против сырого B | −0,00465 | [−0,00769; −0,00157] |
| поправка против «только уровень» | −0,00167 | [−0,00366; +0,00034] |

Вывод: главный выигрыш — уровень (B занижает тотал на картах 2+), ему нужен только номер карты, который
боту виден вживую (`series_game_number` в `runtime/sourcetv_matches.json`). Темп сверх уровня в этой форме
не доказан. План: тень в журнале панели с обоими вариантами (уровень и уровень+темп) + живой журнал
килов карт по `series_id`, решение владельца после живого замера. Тестовых предсказаний E281 (гейт
ставок `kills_total`) нет — перенос на E281 не измерен.

**Где искать ошибку (дополнение).** Живой счёт `radiant_score + dire_score` — очки команд, а офлайн-признак —
сумма килов игроков `pstats`; разница (убийства крипами/башнями) сдвигает признак на несколько килов.
Параметры подобраны на тех же картах, где измерялся post-hoc эффект: независимого подтверждения нет
(свежий период после 11.08 — 1 371 карта в корпусе, мощности не хватает).

## 9. Stage A в проде 25.09: тень в журнале панели

**Поправка к §8.** Номер карты из SourceTV вживую ненадёжен. Захват `runtime/sourcetv_matches.json` раз в
60 с (24.09, 21:57–01:57 МСК, 240 снимков) показал две карты одного bo3, 9014406398 и 9014519155. У обеих
`series_game_number = 2` и счёт серии 1:0, `series_id = None`. Поэтому карты серии связываются по времени:
та же неупорядоченная пара team ID (или `series_id`, если он есть), разные `match_id`, прошлая карта
пропала из ленты не позже чем через 60 с после первого появления текущей, разрыв ≤ 4 ч, `series_type`
совпадает. `series_game_number ≥ 2` остаётся только запасным признаком «это продолжение» для `p_level`.

**Что выкачено** (c26ebca, рестарт 02:23:17 МСК):
- `base/series_tempo.py` — ledger `runtime/series_tempo_ledger.json`.
- `metadata.series_tempo` на вердикте `total_55_50` в `runtime/ml_panel.jsonl`: `p_level`, `p_tempo`,
  `n_prev`, `prev_total_mean`, `link`, `continuation_source`.
- Текст панели, вероятность, ставки и диспетчер не меняются (hard-verifier C1: вывод с тенью и без
  совпадает). Выключатель — `SERIES_TEMPO_SHADOW=0`.
- Доказательства: `.orchestra/evidence/series-tempo-stageA/`.

**Как мерить дальше.** Через 1–2 дня живых карт посчитать:
- покрытие: доля карт панели с `series_tempo`, доля продолжений с `n_prev ≥ 1` против
  `continuation_source = sourcetv_game_number`;
- log-loss p / `p_level` / `p_tempo` на доигранных картах (исход — финальный тотал из ledger или OpenDota).

Решение о гейте или показе — за владельцем.

**Где искать ошибку.**
- Две серии одной пары в пределах 4 ч могут склеиться.
- Если карта N+1 появляется меньше чем через ~3 мин после последнего изменения карты N в ленте, связи нет.
  В захвате хвост карты длился 4,1 мин, следующая карта пришла через 29 мин.
- `first_seen` — момент, когда бот впервые увидел карту, а не старт карты.
- Счёт ленты — очки команд, а не сумма килов игроков, как в офлайн-признаке.

## 10. 25.09 07:00: поправка боевая, B в карточке вместо E281

Владелец: «выкатывай в прод без shadow», затем «замени старую модель на новую». Коммит 0f1858d, рестарт 07:00:11 МСК.

**Что в карточке.**
- Вероятность B `total_55_50` на продолжениях серии заменена исправленной:
  - `p_tempo`, если прошлые карты серии есть в ledger;
  - `p_level`, если продолжение видно только по `series_game_number ≥ 2`.
- Сторона, 🟢, ист. WR и кэф пересчитываются по спецификации B.
- В блоке 🤖 ML после окон — строки B «радиант ≥30» (≥30 против ≤25) и «тотал ≥55» (≥55 против ≤50), без
  блока E281. Строки «Dire ≥30» у B нет. Если B не посчиталась, остаётся блок E281.
- Вход гейта ставок `kills_total` (E281 `kills30`) не менялся.

**Откаты без деплоя:** `ML_PANEL_KILLS_DISPLAY=e281`, `SERIES_TEMPO_APPLY=0`.

**Смысл журнала изменился.** С 07:00 `p` у `total_55_50` в `runtime/ml_panel.jsonl` — показанная (исправленная)
вероятность. Вероятность модели — `metadata.series_tempo.p_raw`; флаги — `applied`, `variant`, `skip`.
Инструменты, которые мерят саму модель по журналу (`base/evaluate_kills_replay.py:88`,
`base/kills_forward_protocol.py:103-104`), должны брать `p_raw`, если он есть.

**Где искать ошибку.**
- Поправка применяется только при `model == "B_kv3"` и найденной спецификации `kv3_panel_serving._specs`.
  Если спецификации нет, в журнале `skip=spec_unavailable`, и поправка молча не действует.
- Доказательства: `.orchestra/evidence/series-tempo-b-display/`.
