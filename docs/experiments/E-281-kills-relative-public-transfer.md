---
id: E-281
title: "Тотал и килы стороны: относительные профили паблика и адаптация на про"
date: "2026-09-12"
area: ml
status: full
corpus: "Raw STRATZ public 7.41: только завершившиеся до 01.06.2026; pro: январь-сентябрь 2026"
verdict: "Полный прогон: 2.329M pub, pro test 687 карт. Выбранный контроль снизил log loss 0.5899→0.5793 / 0.6358→0.6299, CI включают ноль. Относительные deaths ухудшили side. Большого прироста и превосходства над production-панелью не установлено."
harness: "base/train_kills_transfer.py, base/kills_transfer_data.py; план runtime/artifacts/kills/relative_transfer/extract_plan.json"
---

# E-281. Относительные профили героев и их команд

Запрос: обучить модели ≥ килов карты и стороны на пабликах, использовать
нетворс, опыт, убийства и смерти героя отдельно от команды с ним; абсолютный
уровень паблик-килов не переносить на про. Допускается дообучение на про.

## Подтверждённые дефекты и ограничения прежнего пути

- `build_panel_models.build_targets`: ≥55 против ≤50 и ≥30 против ≤25.
  Промежуточные значения исключены; это условные вероятности, а не обычные
  вероятности превышения порога на всех картах.
- Та же функция обучает только Radiant; комментарий признаёт отсутствие
  зеркального обучения общей модели стороны.
- `causal_priors.PRIOR_NAMES` содержит командные килы, NW/XP, но не deaths.
- Cached `window_features_v2.parse` подставляет нули при пропущенной статистике
  и требует наличия хотя бы одного окна. Для новой цели итоговых килов raw
  извлекается заново без фильтра по наличию поминутного разбора.
- В пабликах нет постоянной организации. Команда с героем означает сторону
  конкретной карты; отдельная история team ID возможна только на про.

## Замороженный план проверки

Паблики до 01.06.2026; новые признаки хранят личные доли K/D/A/NW/XPM/GPM,
отношения к темпу всей карты, командные доли и участие в убийствах. Смерти
команды вычисляются по смертям её игроков, а не подменяются киллами соперника.
Пропуск остаётся NaN; нулевой знаменатель не означает нулевой показатель.
Герои упорядочены по стороне и позиции; ID разрежены, верхней границы 130 нет.

Про-обучение: июнь-июль; валидация: 01-19 августа; тест: с 20 августа.
Разделение по завершению матча; серия не должна пересекать train/val/test.
Гиперпараметры выбираются только на валидации, затем тест читается один раз.
Общий target стороны обучается в двух ориентациях, обе остаются в одном split.
Основные цели включают всю середину: total≥55 и side≥30. Сравнение с текущей
панелью пока не установлено: её полный исторический набор признаков здесь
не воспроизведён. Результат против внутренних baseline этого не доказывает.

Публичное предобучение: Ridge на героях, ролях и парах. Цель total — отношение
киллов к среднему завершившихся карт за предыдущие 28 дней, зафиксированному
на начало недели; одинаковая причинная нормировка в train и validation.
Цель стороны — разность долей киллов. Холодный период исключается; доли и
профили не используют итог текущего pro-матча.

На про сравниваются пять вариантов: только pro-истории, public draft,
относительные public-профили, те же профили без признаков смертей и абсолютные
public-профили как контроль. Все используют недавние завершившиеся истории
игроков и команд. Профили хранят поддержку и покрытие, неизвестный герой
получает нейтральное отклонение от роли и нулевую поддержку.

Августовская валидация разделена на три последовательные части: ранняя
остановка, калибровка и выбор варианта/глубины. Серии на границах исключаются.
CatBoost: depth 4/6, максимум 800 итераций; выбор по калиброванному log loss.
Отчёт содержит AUC, log loss, Brier, accuracy и интервал разницы log loss с
двумя внутренними baseline, bootstrap по дням. Две стороны карты группируются.

## Воспроизведение

```sh
/Users/alex/Documents/ingame/venv_catboost/bin/python3 -m pytest base/tests/test_kills_relative_profiles.py base/tests/test_kills_transfer_features.py -q
/Users/alex/Documents/ingame/venv_catboost/bin/python3 .orchestra/runtime/orchestra.py resources preflight --plan runtime/artifacts/kills/relative_transfer/extract_plan.json
/Users/alex/Documents/ingame/venv_catboost/bin/python3 .orchestra/runtime/orchestra.py resources run --plan runtime/artifacts/kills/relative_transfer/extract_plan.json --background
```

Планы включают SHA-256 исходников и raw-файлов. Каждый файл извлечения имеет
контрольную точку; повторный запуск проверяет SHA и использует готовый результат.
Результаты обучения, exact run IDs и проверки приведены ниже.
Production runtime и активные артефакты панели не изменяются этим экспериментом.

После extraction создать training manifest с SHA полученных summary/chunk NPZ,
исходников `kills_*`, `train_kills_transfer.py`, `draft_features.py` и версиями
зависимостей. Worker argv:

```sh
{python} base/train_kills_transfer.py --public-rows <public-output-dir> --pro-rows <pro-output-dir> --output-dir {output_dir} --threads {threads}
```

Это argv для общего executor, а не разрешение обходить resource preflight.

## Текущий результат, 12.09.2026

- Первоначальный запуск был заблокирован. Preflight проверил входные
  SHA/dependencies, но отказал по CPU: local load 15.6 при 10 CPU, reserve 50%.
  В resources.json включён только local; чужой aitrading sweep уже использует
  восемь workers. Его процессы не менялись. Повторное наблюдение load 12.4
  также не даёт свободного бюджета. Нужны свободные ресурсы или явное исключение.
- 14 регрессионных тестов прошли, включая небольшой сквозной корпус:
  public pretraining, все пять вариантов двух целей, сохранение/загрузка и
  совпадение воспроизведённых test predictions. Это техническая проверка,
  не оценка качества на реальных матчах.
- Независимая проверка выявила и после исправления подтвердила два места:
  одну причинную нормировку public train/validation и независимые части pro
  early stopping/calibration/selection.
- Улучшение точности и готовность замены production-моделей **не установлены**.

### Полное извлечение после освобождения ресурсов

В 15:47 MSK повторный preflight прошёл без исключений. Campaign
`run-9a56df1aa667647b14fb2f32` завершил оба задания на local с exit 0;
`all_resources_contributed=true`. Извлечено 2 329 097 public-записей и 46 099
pro-записей. Проверка SHA всех 288 chunk NPZ прошла. После удаления точных
повторов осталось 45 297 pro-карт, конфликтующих повторов нет. Обнаружены 134
public/pro пересечения; training исключает их из пабликов, оставляя 2 328 963.
Все сохранённые K/D/A/NW/XPM/GPM присутствуют. Средний total публичной выборки
77.27 против 68.14 в pro train (test outcomes для этой проверки не использованы).

После временных границ и исключения пересекающих их серий: 11 164 train,
1 568 validation, 687 test. Части валидации: 515 early-stop, 514 calibration,
519 selection; остальные карты исключены на внутренних границах. В pro нет
неизвестных ID серии/команды. Полный протокол проверки:
`runtime/artifacts/kills/relative_transfer/corpus_validation.json`.

Manifest обучения: `runtime/artifacts/kills/relative_transfer/train_plan.json`,
296 хэшей исходников/NPZ/summary и восемь точных версий зависимостей. Пиковые
временные матрицы public draft ограничены порциями по 25 000 строк; отдельный
тест подтверждает точное совпадение с полным encoder для обеих ориентаций.
Вместе со сквозным smoke — 15 тестов пройдено. Полное обучение и его результат
фиксируются только по терминальной receipt и сохранённым test predictions.

### Завершённое обучение и результат

User явно разрешил запуск при прежнем отказе resource preflight, затем также
разрешил несколько потоков при необходимости. Уже начатый прогон завершён
в одном потоке: `run-2536be97ce82c95f51ef5752`, job `train`, PID 15716,
exit 0, 379.57 секунды. После первого dispatch общая конфигурация ресурсов
восстановлена; только campaign хранит разрешённое исключение. Audit:
`runtime/artifacts/kills/relative_transfer/train_resource_exception.json`.

Паблик-предобучение после warmup: 1 814 068 train и 465 795 validation;
48 238 начальных строк без причинной нормы исключены. Оба Ridge выбрали
alpha=1000, размер словаря 16 764. Про-выбор варианта завершён на независимой
selection-части; для обеих целей выбран `absolute`. Это контроль с абсолютными
публичными профилями и последующим обучением/калибровкой на про, а не перенос
публичного порога или публичной вероятности непосредственно в pro.

Тест: 687 карт за 23 дня, 1374 наблюдения стороны. В таблице log loss ниже —
лучше; AUC выше — лучше. `pro_only` — внутренний baseline с героями и историей
про-команд/игроков, **не работающая production-панель**.

| Вариант | Total log loss | Total AUC | Side log loss | Side AUC |
|---|---:|---:|---:|---:|
| pro_only | 0.58986 | 0.72020 | 0.63583 | 0.67892 |
| public_draft | 0.57756 | 0.73429 | 0.63022 | 0.68523 |
| relative | 0.57934 | 0.73007 | 0.63686 | 0.67594 |
| relative_no_deaths | 0.58029 | 0.73073 | 0.63313 | 0.68106 |
| absolute — выбран на validation | 0.57934 | 0.73043 | 0.62986 | 0.68652 |

Для выбранного варианта accuracy total 69.14% против 68.12% baseline;
side 64.05% против 62.52%. Brier total 0.19842 против 0.20210;
side 0.22062 против 0.22334.

Парная разница log loss выбранного варианта относительно `pro_only`:

- Total −0.01051, 95% day-cluster bootstrap CI [−0.02259; +0.00266].
- Side −0.00597, CI [−0.01276; +0.00063].

Оба интервала включают ноль: устойчивый прирост выбранного варианта не доказан.
Сложные профили также не превосходят простой `public_draft`: total difference
+0.00178 [−0.00247; +0.00531], side −0.00036 [−0.00506; +0.00388].
Отдельное сравнение `public_draft` с `pro_only` даёт total −0.01230
[−0.02276; −0.00058], side −0.00561 [−0.01268; +0.00190]. Эти дополнительные
интервалы не корректировались за множественные сравнения; их нельзя использовать
для выбора нового победителя по уже открытому тесту.

Абляция смертей: `relative` относительно `relative_no_deaths` даёт total
−0.00095 [−0.00682; +0.00362], side **+0.00373** [+0.00054; +0.00669].
В данном представлении deaths не помогли total и ухудшили side. Это вывод о
проверенном представлении, а не доказательство бесполезности смертей вообще.

Проверка примеров пользователя в замороженных pub-профилях (сглаженные средние;
показывается наиболее частая позиция героя):

| Герой, позиция | Число наблюдений | Доля смертей команды у героя | Среднее той же роли |
|---|---:|---:|---:|
| Lone Druid, 1 | 79 249 | 11.19% | 15.20% |
| Bounty Hunter, 4 | 87 030 | 24.47% | 23.84% |
| Undying, 5 | 91 389 | 25.56% | 24.42% |

Наблюдения согласуются с описанной индивидуальной смертностью, но не доказывают
механическую причинность и не гарантируют перенос в итоговый прогноз.

Артефакты моделей, калибраторов, profiles и predictions:
`.orchestra/jobs/run-2536be97ce82c95f51ef5752/train/output/`.
Основные файлы `summary.json`, `{total,side}_selection.json`,
`{total,side}_test_predictions.npz`, `{total,side}_{absolute,relative}.cbm`.
Команда проверки завершения:

```sh
/Users/alex/Documents/ingame/venv_catboost/bin/python3 .orchestra/runtime/orchestra.py resources status --run run-2536be97ce82c95f51ef5752
```

Логи: `.orchestra/jobs/run-2536be97ce82c95f51ef5752/train/stdout.log` и
`stderr.log`. В stderr есть RuntimeWarning от матричной операции sklearn
LogisticRegression. Восемь сохранённых калибраторов отдельно проверены на своих
calibration-строках: все параметры/прогнозы конечны, max abs градиента среднего
регуляризованного objective ≤1.091e−5, ниже tol=1e−4. Это подтверждает сходимость
проверенных результатов, но причина предупреждений численного окружения
не установлена. Проверка: `runtime/artifacts/kills/relative_transfer/calibration_verification.json`.
Дополнительные интервалы и примеры героев: `ablation_intervals.json` и
`hero_examples.json` в том же каталоге.

Независимая проверка подтвердила SHA/размеры трёх outputs из receipt и
канонический hash плана. Метрики всех пяти вариантов воспроизведены из
сохранённых predictions; для `absolute` и `relative` обеих целей повторный
прогноз сохранённых CatBoost и калибраторов совпал точно, max difference 0.
Для `relative_no_deaths` проверены только predictions и метрики: его модель
не сохранялась. Все вероятности конечны и лежат в [0,1]; две стороны каждой
карты остаются вместе.

Профильные тесты: 15 passed. Дополнительный запуск
`python -m pytest -q base/tests/test_pipeline_integrity.py` завершился exit 1:
187 passed, 17 failed. Падения относятся к существующим проверкам runtime
HTML/dispatch/proxy/lanes/ELO, которые не импортируют новые kills-модули;
этот эксперимент их не менял. Полная suite не объявляется зелёной.
Лог сохранён в `runtime/artifacts/kills/relative_transfer/pipeline_verification.log`.

Итог: обучение и офлайн-проверка завершены; **запрошенного большого прироста
не получено**. Production не заменён. Для дальнейшего решения нужны сопоставимый
replay текущей панели и новый временной holdout. После открытия этого теста
его повторное использование для новых вариантов будет exploratory, а не
новой независимой проверкой. Отдельная гипотеза следующей итерации — сохранять
темп карты при нормировке личных и командных K/D относительно прошлых публичных
матчей: нынешние доли внутри карты этот общий масштаб убирают. Здесь она
не проверялась и улучшением не объявляется.

## Где искать ошибку

1. Утечка будущих пабликов в более ранний pro test; cutoff применяется к end.
2. Нули вместо отсутствующих K/D/NW/XP, включая старые кэши и знаменатели долей.
3. Самозасев справочника на своих train-строках; справочник должен быть раньше.
4. Различие суммы личных килов и официального scoreboard: текущая панель тоже
   использует player kills; это явно фиксируется как определение label.
5. Смешение разных порогов/выборок, матчей одной серии и двух ориентаций.
6. Новый scorer без прежних сильных блоков нельзя выдавать за улучшение всей
   текущей панели только по сравнению со слабым baseline.
