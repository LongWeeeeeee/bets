[Service]
# New host has 15G RAM; default auto disables sqlite when RAM>8G and tries full JSON (deleted).
Environment=STATS_LOOKUP_BACKEND=sqlite
Environment=STATS_SHARDED_LOOKUP_MAX_RAM_GB=32
Environment=STATS_SEQUENTIAL_WARMUP_ENABLED=1
